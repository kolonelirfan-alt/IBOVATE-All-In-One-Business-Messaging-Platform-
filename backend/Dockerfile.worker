FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy source code
COPY backend/ .

# Run the worker using rq
CMD ["rq", "worker", "webhook_tasks", "--url", "${REDIS_URL}"]
